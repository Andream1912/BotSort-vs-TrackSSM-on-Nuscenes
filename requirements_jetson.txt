# Jetson-friendly requirements.
# Notes:
# - Do NOT install torch/torchvision/opencv-python here: use the NVIDIA-provided wheel/base image.
# - Keep this aligned with requirements.txt where possible.

numpy>=1.24.0
tqdm>=4.65.0
easydict>=1.10
pyyaml>=6.0
trackeval>=1.0.0
scipy>=1.10.0
Pillow>=9.5.0

# Evaluation / analysis
pandas>=2.0.0
motmetrics>=1.4.0

# NuScenes dataset utilities
nuscenes-devkit>=1.1.11
pyquaternion>=0.9.9

# Plotting (optional but used by included scripts)
matplotlib>=3.7.0
seaborn>=0.12.0

# YOLOX fine-tuning scripts
loguru>=0.7.0

# Optional detector wrapper (not required for the main YOLOX path)
ultralytics>=8.0.0
